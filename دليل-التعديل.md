# دانا تيليكوم — دليل التشغيل والتعديل

موقع تعريفي احترافي مبني بـ **Next.js 16 + Tailwind CSS v4**، بدعم كامل للعربية (RTL).

## التشغيل

```bash
npm run dev      # وضع التطوير  →  http://localhost:3000
npm run build    # بناء نسخة الإنتاج
npm run start    # تشغيل نسخة الإنتاج بعد البناء
```

> ملاحظة تقنية: نستخدم مُترجم **webpack** (عبر `--webpack` في `package.json`)
> بدلًا من Turbopack، لأن Turbopack يفشل عند وجود أحرف غير لاتينية في مسار
> المجلد (مثل «Üsküdar» و«Masaüstü»). لا تُعِد هذا الخيار إلى Turbopack ما لم
> يُنقل المشروع إلى مسار بأحرف إنجليزية فقط.

## أين أعدّل المحتوى؟

كل النصوص والبيانات القابلة للتعديل موجودة في ملف واحد:

### `src/lib/site.ts`
- **بيانات التواصل** (`contact`): الهاتف، واتساب، البريد، العنوان، ساعات العمل،
  روابط السوشيال ميديا. ← **مهم: بدّل الأرقام والروابط المبدئية بأرقامك الحقيقية.**
  - `whatsapp` يجب أن يكون بالصيغة الدولية بدون «+» أو مسافات، مثال: `963991234567`.
- **الخدمات** (`services`): العناوين والوصف والنقاط.
- **الباقات** — ثلاثة أنواع، كلٌّ له متغيّر خاص:

  | المتغيّر | النوع | ما يجب تعديله |
  |---|---|---|
  | `speedPlans` | باقات بسرعة ثابتة | السرعة والسعر لكل باقة. الباقة الأولى مضبوطة على **6$** (الأدنى)، والباقيتان `—` بانتظار قيمك |
  | `usagePlan` | حسب الاستهلاك | `pricePerGb` — سعر الجيجابايت الواحد |
  | `cardBundles` | البطاقات | الحزمة والسرعة والسعر. البطاقة الوسطى مضبوطة على **50 جيجا / 5 ميغا**، والباقيتان `—` |

  كل قيمة مكتوب مكانها `—` تعني: **بانتظار رقمك الحقيقي**.
  أما `packageTypes` فهو وصف الأنواع الثلاثة الظاهر في الصفحة الرئيسية.
- **مناطق التغطية** (`coverage`).
- **الأسئلة الشائعة** (`faqs`).

## 🔐 صفحة تسجيل الدخول للشبكة (MikroTik Hotspot)

الملف: `login.html` (النسخة الأصلية محفوظة في `login.original.html`).

### ما تم الحفاظ عليه بالكامل (لا تعدّله)
- كل متغيّرات MikroTik الـ23: `$(link-login-only)` · `$(link-orig)` · `$(chap-id)` ·
  `$(chap-challenge)` · `$(error)` · `$(username)` · `$(mac-esc)` · `$(link-orig-esc)` …
- دالة `doLogin()` وتشفير **CHAP-MD5** كما هي حرفيًا.
- النموذج المخفي `sendin` وأسماء الحقول: `username` · `password` · `dst` · `popup`.
- الرابط `/md5.js` (مدمج داخل MikroTik).
- رابط الإنترنت المجاني (trial) ورسائل الخطأ.

### ما تغيّر
- تصميم كامل بهوية دانا تيليكوم (كحلي/أصفر) مع بطاقة زجاجية وهالات ضوئية.
- **كل التنسيقات مضمّنة داخل الملف** — لأن مستخدم الـHotspot لا يملك إنترنت بعد،
  فأي ملف من الإنترنت (خطوط جوجل مثلًا) لن يُحمّل.
- الأيقونات صارت **SVG مضمّنة** — لم تعد بحاجة إلى:
  `css/style.css` · `img/user.svg` · `img/password.svg`

### الملفات المطلوبة على الراوتر
| الملف | ملاحظات |
|---|---|
| `login.html` | ارفعه إلى مجلد `hotspot` |
| `img/logo-compressor.png` | شعارك الحالي — **يبقى كما هو** (يظهر داخل دائرة بيضاء لذا يعمل بأي لون) |
| `/md5.js` | موجود في MikroTik تلقائيًا |

### معاينة قبل الرفع
شغّل الموقع محليًا ثم افتح:
- `http://localhost:3000/login-preview.html` — الشكل الطبيعي
- `http://localhost:3000/login-preview-error.html` — شكل رسالة الخطأ

> ملفات المعاينة للعرض فقط — **لا ترفعها على الراوتر**، ارفع `login.html`.

## الشعار

الشعار الأصلي الذي رفعته: `public/logo-كحلي.png` (9000×9000 بكسل — 592 كيلوبايت).
لأنه ضخم جدًا للويب وبلون كحلي فقط، وُلِّدت منه نسخ مُحسّنة:

| الملف | الاستخدام | الحجم |
|---|---|---|
| `public/logo-mark.png` | الشعار (الرمز) على الخلفيات الفاتحة | ~29 KB |
| `public/logo-mark-white.png` | نسخة بيضاء للخلفيات الداكنة (الهيدر فوق الهيرو + الفوتر) | ~17 KB |
| `public/logo-full.png` | الشعار الكامل مع كلمة Dana Telecom | ~78 KB |
| `src/app/icon.png` | أيقونة المتصفح (favicon) — الرمز أبيض على مربّع كحلي | ~24 KB |
| `src/app/apple-icon.png` | أيقونة أجهزة آبل | ~11 KB |

> إن غيّرت الشعار مستقبلًا، استبدل هذه الملفات بنفس الأسماء.
> يمكنك حذف `logo-كحلي.png` الأصلي إن أردت تقليل حجم النشر (لم يعد مستخدمًا).

## الخريطة التفاعلية

مبنية بمكتبة **Leaflet** مع خرائط OpenStreetMap (مجانية، بدون مفتاح API).

- الملفات: `src/components/CoverageMap.tsx` (الخريطة) و`CoverageExplorer.tsx` (التبويبات والمفتاح والنِّسب).
- البيانات في `src/lib/site.ts` ضمن `coverageCities`:
  - `wifiPercent` — نسبة تغطية الواي فاي الإجمالية (الدانا 80، خان العسل 50)
  - `wifiSpots` — **الأحياء** (أصفر): لكل حي `name` و`lat/lng` و`radiusM` و`percent`.
    شفافية اللون على الخريطة تزداد مع ارتفاع نسبة الحي.
  - `nanobeamRadiusM` — نصف قطر تغطية NanoBeam (أخضر) الذي يشمل المدينة وما حولها.
  - `center` و`zoom` — مركز الخريطة لكل مدينة.

### الأحياء الحالية (مدينة الدانا)

| الحي | النسبة |
|---|---|
| الحي الأوسط | 95٪ |
| الحي الشمالي | 85٪ |
| الحي الغربي | 80٪ |
| الحي الجنوبي | 60٪ |

المتوسط = **80٪** (النسبة الإجمالية للمدينة). إن عدّلت نسب الأحياء، حدّث
`wifiPercent` ليبقى متوافقًا.

### زر «هل موقعي ضمن التغطية؟»

يستخدم `navigator.geolocation` ويقارن موقع الزائر بالأحياء ونطاق NanoBeam
(الحساب في `src/lib/geo.ts`). النتائج الثلاث: داخل الواي فاي (مع اسم الحي) /
ضمن NanoBeam / خارج التغطية.

> ⚠️ **تحديد الموقع يعمل فقط عبر `https` أو `localhost`** — هذا شرط أمني في كل
> المتصفحات. عند فتح الموقع من رابط `http://` على الشبكة المحلية سيظهر تنبيه
> يشرح ذلك. بعد رفع الموقع على استضافة بشهادة SSL سيعمل تلقائيًا.

> ⚠️ **الإحداثيات تقريبية** — عدّلها لتطابق مواقع نقاط البث الحقيقية عندك.
> تجد الإحداثيات بسهولة من خرائط جوجل (زر يمين على الموقع → نسخ الإحداثيات).

## الأيقونات

الموقع يستخدم أيقونات متجهة (SVG) من مكتبة **lucide-react** — لا رموز إيموجي.
كل الأيقونات مُعرّفة في مكان واحد: `src/components/icons.tsx`

لإضافة أيقونة جديدة:
1. استوردها من `lucide-react` في أعلى الملف.
2. أضفها إلى `iconRegistry` باسم مفتاح واضح.
3. استخدمها في أي صفحة: `<Icon name="اسم_المفتاح" size={24} />`

## الصور

الصور موجودة في `public/images/`:

| الملف | الاستخدام الحالي |
|---|---|
| `tower.jpg` | صورة الهيرو في الرئيسية + صفحة التغطية |
| `dish.jpg` | بطاقة Bento الكبيرة + بطاقة NanoBeam + بطاقة خدمة NanoBeam |
| `home.jpg` | بطاقة خدمة الإنترنت اللاسلكي |
| `cctv.jpg` | صفحة الكاميرات + بطاقة خدمة الكاميرات |
| `technician.jpg` | صفحة من نحن |
| `network.jpg` | صفحة الخدمات (شريط الدعوة للتواصل) |

## 💳 وسائل الدفع وشام كاش

تظهر وسائل الدفع في صفحة **الباقات والأسعار** (قسم «وسائل الدفع»)،
مع رابط سريع من صفحة **تواصل معنا**.

عدّلها في `src/lib/site.ts` ضمن `payments`:

1. **رقم حساب شام كاش** — غيّر `accountNumber` من
   `"⬅︎ أضف رقم الحساب هنا"` إلى رقم حسابك الحقيقي.
2. **رمز QR** — استبدل الملف:
   ```
   public/images/sham-cash-qr.svg
   ```
   بالصورة الحقيقية لرمز QR الخاص بحسابك.
   - إن كانت صورتك بصيغة PNG/JPG، ضعها في نفس المجلد ثم عدّل السطر
     `qr: "/images/sham-cash-qr.svg"` ليصبح مثلًا
     `qr: "/images/sham-cash-qr.png"`.

> ⚠️ الرمز الحالي **صورة توضيحية فقط وليست رمز دفع فعّال**. لن تعمل عمليات
> الدفع حتى تضع رمز حسابك الحقيقي.

## ⚠️ آراء العملاء — يجب استبدالها

قسم «ماذا يقول عملاؤنا» في الصفحة الرئيسية يستخدم حاليًا **نصوصًا توضيحية
فارغة وليست آراء حقيقية**. عدّلها في `src/lib/site.ts` ضمن `testimonials`
واستبدلها بآراء عملاء فعليين وبإذنهم قبل نشر الموقع.

إن لم تتوفر لديك آراء بعد، احذف القسم مؤقتًا من `src/app/page.tsx`
(القسم المعنون «آراء العملاء»).

الصور الحالية من **Unsplash** (مجانية للاستخدام التجاري بدون إسناد).

> ⚠️ **مهم:** استبدلها بصور حقيقية لأبراجكم وتركيباتكم وفريقكم — الصور الحقيقية
> تعطي مصداقية أعلى بكثير من الصور الجاهزة. يكفي استبدال الملف بنفس الاسم في
> `public/images/` دون تعديل أي كود.

## الألوان والهوية

معرّفة في `src/app/globals.css` ضمن كتلة `@theme`:
- الكحلي: `--color-navy: #282369`
- الأصفر: `--color-brand: #fff200`

## الصفحات

الرئيسية · من نحن · خدماتنا · مناطق التغطية · الباقات والأسعار ·
كاميرات المراقبة · الأسئلة الشائعة · تواصل معنا.

## نموذج التواصل

نموذج «تواصل معنا» يرسل الطلب مباشرة عبر **واتساب** (لا يحتاج خادمًا خلفيًا).
عند رغبتك بحفظ الطلبات في بريد إلكتروني أو قاعدة بيانات، يمكن إضافة ذلك لاحقًا.

## أشياء يُنصح بإضافتها لاحقًا
- شعار الشركة الرسمي (استبدال الشعار النصّي في `src/components/ui.tsx`).
- صور حقيقية للفريق/التركيبات (حاليًا نستخدم أيقونات).
- خريطة Google Maps فعلية بدل الخريطة التوضيحية في صفحة التغطية.
- الأسعار الحقيقية للباقات.
