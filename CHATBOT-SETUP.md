# المساعد الآلي (Chatbot) — دليل الإعداد

يعمل على **الموقع فقط** (وليس صفحات الهوت سبوت — لأن مستخدم الهوت سبوت
لا يملك إنترنت بعد فلا يستطيع الوصول للمساعد أصلًا).

يجيب حصريًا عن أسئلة خدمات دانا تيليكوم، ومصدر معلوماته هو `src/lib/site.ts`
نفسه — فأي تعديل في الباقات أو الأسعار أو التغطية ينعكس على إجاباته تلقائيًا
دون لمس كود المساعد.

## الملفات

| الملف | الدور |
|---|---|
| `src/app/api/chat/route.ts` | نقطة API على الخادم — المفتاح لا يصل للمتصفح إطلاقًا |
| `src/lib/chatKnowledge.ts` | يبني قاعدة المعرفة وتعليمات النظام من بيانات الموقع |
| `src/components/ChatWidget.tsx` | نافذة المحادثة (زر أسفل يمين الشاشة) |
| `netlify.toml` | إعداد النشر + تفعيل إضافة Next.js |

---

## ⚠️ الأهم: حصة Gemini المجانية

المفتاح الحالي على **الباقة المجانية**، وحصّتها الفعلية (تم التحقق منها):

```
GenerateRequestsPerDayPerProjectPerModel-FreeTier = 20 طلب / يوم
```

**عشرون طلبًا في اليوم فقط.** أي أن ٥ زوار يسألون ٤ أسئلة يستهلكون الحصة
كاملة، وبعدها يظهر لكل الزوار خطأ «تعذّر الحصول على رد».

**لتشغيل المساعد فعليًا للعملاء يجب تفعيل الفوترة:**
1. افتح: https://aistudio.google.com/apikey
2. اربط المشروع بحساب فوترة (Pay-as-you-go)
3. النموذج المستخدم `gemini-2.5-flash` رخيص جدًا للأسئلة القصيرة

بدون ذلك، المساعد سيتوقف بعد ~20 سؤالًا يوميًا.

---

## إعداد المفتاح

### محليًا
ملف `.env.local` (مستثنى من Git تلقائيًا عبر `.gitignore`):
```
GEMINI_API_KEY=your_key_here
```

### على Netlify
```
Site configuration → Environment variables → Add a variable
  Key    : GEMINI_API_KEY
  Value  : مفتاحك
  Scopes : Functions   ← ضروري
```
ثم أعد النشر (Trigger deploy → Clear cache and deploy).

> 🚫 لا تضع المفتاح في `netlify.toml` ولا في أي ملف يُرفع إلى Git.

---

## الحماية المطبّقة

- **المفتاح على الخادم فقط** — تم التحقق أنه لا يظهر في HTML ولا في ملفات
  الجافاسكربت المرسلة للمتصفح.
- **حد طلبات**: 12 طلبًا/دقيقة لكل IP (حاجز أولي؛ ذاكرة الدوال المؤقتة لا تدوم
  بين الاستدعاءات على Netlify فهو ليس حماية كاملة).
- **حد طول الرسالة**: 600 حرف.
- **حد سجل المحادثة**: آخر 8 رسائل (توفير تكلفة).
- **مهلة**: 25 ثانية.
- **حصر النطاق**: يرفض أي سؤال خارج خدمات دانا تيليكوم.
- **مقاومة الحقن**: يتجاهل محاولات تغيير تعليماته أو كشفها.

---

## ملاحظة تقنية مهمة (مشكلة تم إصلاحها)

تعليمات النظام تضع بيانات الشركة داخل وسم `<بيانات_الشركة>` ثم القواعد **بعدها**،
وتنتهي بأمثلة قصيرة (few-shot).

السبب: عندما كانت الأسئلة الشائعة مكتوبة بصيغة `س:/ج:` في **نهاية** التعليمات،
كان النموذج يقلّد النمط ويعيد **أول إجابة جاهزة** بدل الرد على سؤال الزائر —
أيًّا كان السؤال. الإصلاح: تحويلها إلى نقاط، ووضع القواعد والأمثلة في النهاية.

---

## التعديل

- **نص الترحيب والاقتراحات السريعة**: `src/components/ChatWidget.tsx`
  (المتغيرّان `GREETING` و `SUGGESTIONS`).
- **سلوك المساعد وقواعده**: `src/lib/chatKnowledge.ts` (دالة `buildSystemPrompt`).
- **المعلومات التي يعرفها**: لا تعدّلها هنا — عدّل `src/lib/site.ts` وستنتقل تلقائيًا.
